In compose file i have removed external:true from below volumes section as \
I want Podman to automatically create the volume the moment you start the container \
(and clean it up if you delete the stack)